# 遺言 — セッション「グラファイトリファクタ」(2026-08-27 凍結)

逐語の会話履歴は同ディレクトリの `2026-08-27-グラファイトリファクタ.txt` にある。本書は後継セッションが作業を続行するための最小の状態記録である。再開手順はスレッド引き継ぎスキルに従うこと。

## 1. 最重要: 進行中のまま残る作業

**issue #12(README再設計とdocs分離)の実装エージェント(Opus実装員)が走行中のまま、このセッションは終わる。**

- 承認済み仕様書: `C:\Users\POKR30~1\AppData\Local\Temp\claude\C--devs-Graphite\db89fb63-2d7a-4e47-9a03-1b163e40615d\scratchpad\issue12_design.md`(10コミット計画。折衷案 = docs/development/ と docs/history/ のみ新設、生成物再生成ゼロが必須条件)
- 凍結時点の痕跡: CLAUDE.md に check-docs 登録済み(コミット1完了済みの証拠)、xtask/src/document_reference.rs 等が編集中間状態
- **後継の検死手順**: `git log --oneline -12` で 534904c より上に積まれたコミット数を確認 → `git status` で作業途中の未コミット変更を確認 → `cargo build`/`cargo test`/`cargo xtask generate --check` で実状態を判定(IDE診断は中間状態の残骸を出すので信用しない。必ず cargo で実測する) → 未完了なら仕様書を渡して実装員(Opus)を再起動(前例: 第7段階で同じ引き継ぎをやって成功した)
- 実装完了後の残り工程: 親検証 → 3検査(Opusレビュー員+構造チェック員+文章チェック員) → 指摘はClaude側実装員が修正(コミットせず) → 親検証 → 親コミット → **push** → issue #12 ノート確認。実装エージェントは外部crate生成手段の別Issueを起票し README の #NN を実番号へ差し替える指示も受けている

## 2. リポジトリの確定状態(push済み)

- main は 534904c まで origin へ push 済み。#1〜#8(実装完了・issueはopenのまま=クローズはオーナー判断)、#9 実装完了、#10 クローズ済み、#11 実装完了(87fb100..534904c、16コミット)
- 非回帰の基準: `cargo test --workspace` 全通過 / `cargo xtask generate --check` 差分ゼロ(生成28ファイルのバイト同一) / graphite単体+統合=151の不変等式 / clippy 残1件(tests/flow.rs:68 ptr_arg、#13スコープ)

## 3. 未着手のissue

- **#13**: graph!/flow! のマクロ実装の責務監査(私が起票)。#10の原則を機械適用せず「偽の境界」を判断する方針がオーナー指定。flow_codegen 79行に3層を切るのは偽の境界になりうる、意味層の置き場は単体テスト可能な graphite-codegen 側が候補、という調査済み所見が issue 本文にある
- **#14**: runtime再整理の残課題3件(KeyedTable位置のnewtype化 / 生成docのlib.rs参照文言6箇所 / 公開範囲の非対称2件)
- 外部crate生成手段のissue(#12実装エージェントが起票予定。未起票なら仕様書g節の文案で起票する)

## 4. 運用(オーナーの恒常指示。メモリ graphite-8dankai-unyo.md にもある)

- 作業が終わったら main を push する / 各issueへ進捗があるたび「何をしたか+なぜそうしたか」をコメントする
- 検収フロー: 実装をコミット→報告→3検査→**指摘はClaude側が working tree で修正しコミットしない**→相手(codex)が再レビューしてコミット。codex不在中はClaude側Opusが実装し親がコミット・push
- codex-Graphite はレートリミットで停止中。復帰時は AgentRoom「Graphite」の記録(連番1414以降)を読ませ、44c663a 以降のクロスレビューから合流してもらう
- AgentRoom の監視は Monitor + `python C:/devs/AIOshaberiMCP/agent_watch.py --source jimbo:Graphite --self Claude-Graphite --format text`(jimbo-watchスキル)。セッション死で止まるので後継が再起動する。presence も更新する
- 検査基準: グローバルCLAUDE.md「Rust表現の選択」(2026-08-26制定、issue #9由来) / 用語統一(役割名・辺・多重度・内部位置・名前付きラッパー・名前付き位置型・呼び出し箇所・凍結) / 文章規範(「引く」直訳・体言止め・変更履歴コメント・注意タグ流用が頻出違反)
- IDE診断(new-diagnostics)はサブエージェント作業の中間状態を拾って偽のエラーを出し続けた。**毎回 cargo build/test で実測してから判断する**(このセッションで6回以上再発)

## 5. 承認済み設計書の場所(scratchpad。セッション削除で消えるので必要なら退避)

`C:\Users\POKR30~1\AppData\Local\Temp\claude\C--devs-Graphite\db89fb63-2d7a-4e47-9a03-1b163e40615d\scratchpad\`
- issue12_design.md(走行中の実装の正)
- issue11_design.md / issue11_12_amendments.md(適用済み・記録)
旧セッション(0e7ce481-…)側: issue9_design.md / issue10_design.md(適用済み・記録)

## 6. その他の申し送り

- workflow_tool_doc_placement.md の追従台帳に Graphite が未登録。#12 完了で追従条件(xtask入口・docs索引・生存/ログ型分類)が全て揃うため、完了後に台帳へ追記してオーナーへ報告する
- issue #1〜#9 のクローズ可否はオーナー判断待ち(実装は全て完了済み)
