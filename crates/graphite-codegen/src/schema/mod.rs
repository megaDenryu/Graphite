//! schema DSL の処理を構文・意味・コード生成の3層に分けて収める。
//!
//! 依存の向きは「構文層 → 意味層 ← コード生成層」である。意味層は構文層の
//! 宣言を読んで Graphite の意味論を確定し、コード生成層はその確定済みの意味を
//! Rust へ写す。コード生成層が構文層のトークンを読み直して意味を判断すること、
//! および意味層が生成コードを持つことを禁じる。

pub mod semantic;
