[package]
name = "graphite-macros"
version = "0.1.0"
edition = "2021"
description = "graphite のための proc-macro (graph_schema!, graph! など)。単独では使わず graphite 経由で利用する"

[lib]
proc-macro = true

[dependencies]
syn = { workspace = true }
quote = { workspace = true }
proc-macro2 = { workspace = true }
