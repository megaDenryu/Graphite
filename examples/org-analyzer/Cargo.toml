[workspace]

[package]
name = "org-analyzer"
version = "0.1.0"
edition = "2021"
description = "Graphite (dynamic_graph_schema!) を使った組織分析ツールの実用example"
publish = false

[dependencies]
graphite = { path = "../../crates/graphite" }
